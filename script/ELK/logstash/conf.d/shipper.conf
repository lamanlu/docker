input { 
    redis {
        host => "redis_local"
        port => 6380
        key => "msg"
        data_type => "list"
        codec  => "json"
    }
} 
output {
    redis {
        host => "redis_broker"
        port => 7000
        data_type => "list" 
        codec  => "json"
        key => "msg_mq"
    }
}